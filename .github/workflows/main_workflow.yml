name: main_workflow

# Ejecuta el workflow en un push a la rama específica
on:
  push:
    branches:
      - nodejs_linter_job  # Asegúrate de que este nombre de rama coincida

jobs:
  linter-execution:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v2

      # Step 2: Linter execution
      - name: Linter execution
        uses: github/super-linter@v4
        env:
          DEFAULT_BRANCH: nodejs_example
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LINTER_RULES_PATH: /
          JAVASCRIPT_ES_CONFIG_FILE: .eslintrc.json
          VALIDATE_JAVASCRIPT_ES: true  # Eliminar el error de sintaxis
